/**
 * _abs - computes the absolute value of an integer
 *
 * 2a: number whose absolute vaue is to be found
 *
 * Return: Absolute value of the number
 */
int _abs(int a)
{
	if (a < 0)
		a *= -1;

	return (a);
}
